---
name: Terminal / tool compatibility report
about: Tell us whether rtl-tty works in your terminal or with your AI tool
labels: compatibility
---

**Terminal and version** (e.g. GNOME Terminal 3.52 / VTE 0.76, Konsole, Tilix, tmux 3.4 inside ...):

**Tool run through rtl-tty** (e.g. claude, gemini, aider, plain shell):

**Result of `bash experiments/scp_demo.sh`** (which of cases 1-7 are right-aligned with correct word order?):

**Screenshot** (please crop out anything private):

**Does `rtl-tty <tool>` fix the tool's output?** yes / partly / no. What still looks wrong?
